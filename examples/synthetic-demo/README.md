# Synthetic promotion example

This directory contains a deliberately fictional, non-personal example of one
candidate learning. After initializing a disposable demo workspace, copy the
`core`, `evidence`, `evals`, and `proposals` folders into its
`.evidence-agent-core/` directory to exercise the review, verify, and promote
commands.

These files contain only synthetic fixtures. Real runtime directories are
ignored by default.
